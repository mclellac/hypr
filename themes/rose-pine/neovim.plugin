return {
  "rose-pine/neovim",
  name = "rose-pine",
  lazy = false,
  priority = 1000,
  config = function()
    require("rose-pine").setup({
      variant = "dawn",
      disable_italics = true,
    })
    vim.cmd("colorscheme rose-pine")
  end,
}
return {
  "rose-pine/neovim",
  lazy = false,
  priority = 1000,
  config = function()
    require("rose-pine").setup({
      variant = "dawn",
      disable_italics = true,
      -- styles = {
      --   bold = true,
      --   italic = false,
      --   transparency = true,
      -- },
      -- highlight_groups = {
      --   Comment = { italic = true },
      -- }
    })
    vim.cmd("colorscheme rose-pine")
  end,
}
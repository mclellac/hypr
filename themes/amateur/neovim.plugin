return {
  name = "amateur",
  dir = vim.fn.expand("~/.config/hypr/current/theme"),
  lazy = false,
  priority = 1000,
  config = function()
    -- Load the colorscheme file
    dofile(vim.fn.expand("~/.config/hypr/current/theme/colors/amateur.lua"))
    vim.cmd("colorscheme amateur")
  end,
}

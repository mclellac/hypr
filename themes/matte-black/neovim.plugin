return {
  "ficcdaf/ashen.nvim",
  name = "matte-black",
  lazy = false,
  priority = 1000,
  config = function()
    require("ashen").setup({
      transparent = false,
      styles = {
        comment = { italic = false },
      },
    })
    vim.cmd("colorscheme ashen")
  end,
}
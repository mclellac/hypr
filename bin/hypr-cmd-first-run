#!/bin/bash

set -e

FIRST_RUN_MODE=~/.local/state/hypr/first-run.mode

if [[ -f "$FIRST_RUN_MODE" ]]; then
  rm -f "$FIRST_RUN_MODE"
  $hypr_PATH/install/first-run/battery-monitor.sh
  $hypr_PATH/install/first-run/firewall.sh
  sudo rm -f /etc/sudoers.d/first-run
fi

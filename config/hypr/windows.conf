# Window rules for floating windows launched by hypr-exec
# Re-uses the 'floating-window' tag defined in default/hypr/apps/system.conf
# to apply float, center, and size 800 600.
windowrule = tag +floating-window, class:^(hypr-exec)$
